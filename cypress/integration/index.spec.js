describe("Mine", () => {
  it("mines a JSON-LD document", () => {
    expect(true).to.equal(false);
  });

  it("doesn't mine an invalid JSON-LD document", () => {
    expect(true).to.equal(false);
  });

  it("mines a string", () => {
    expect(true).to.equal(false);
  });
});
